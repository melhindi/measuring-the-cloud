# MTU Experiments

These opt-in UDP profiles use payloads below the expected jumbo-frame path MTU:
8,900 bytes for AWS and 8,800 bytes for GCP. Run them explicitly with
`--benchmark`; do not add them to the provider-neutral full suite.
