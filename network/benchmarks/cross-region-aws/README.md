# AWS Cross-Region Benchmarks

This suite mirrors `network/benchmarks/full` for AWS VPC-peering paths. Its
jumbo UDP payload is 8,300 bytes, conservatively below the inter-region VPC
peering MTU. Keep provider-neutral benchmark suites unchanged.
