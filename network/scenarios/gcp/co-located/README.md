# Network Co-Located Scenarios

This folder contains a focused subset of network scenarios where the client and
server are requested to run on the same host within one availability zone.

Use this folder when you want to isolate the effect of co-location without
running the broader scenario matrix.